// camel case (mostly used in JS)
const myAge = 22;
console.log(myAge);

//snake case (mostly used in c/c++/python/php)
const my_address = "Kolkata, India";
console.log(my_address);

/**
 * NOTE : 1. Class name has to be started with capital letter 
 * 2. reserved word cannot be used as a varible name
 * 3. variable name cannot be started with a number
 */

// Camel case: first letter will be lowercase and remaining word's first letter will be upper case
// Snake case: words will be concatenated with uderscore(_) and each word will be in lower case 